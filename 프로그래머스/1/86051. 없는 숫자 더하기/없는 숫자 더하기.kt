class Solution {
    fun solution(numbers: IntArray): Int {
        // var result = 45
        // numbers.map { result -= it }
        // return result
        
        
        return 45 - numbers.sum()
    }
}